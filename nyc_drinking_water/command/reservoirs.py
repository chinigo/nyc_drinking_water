from logzero import logger

from . import Base

class Reservoirs(Base):
    def _run(self):
        logger.error('As yet unimplemented.')
        # Define database structure for reservoirs
        # Insert data for which tiger_water segments belong to which reservoirs
        pass
